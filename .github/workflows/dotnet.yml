name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Install dependencies
      run: |
        dotnet add package Microsoft.EntityFrameworkCore -Version 5.0.13
        dotnet add package Microsoft.VisualStudio.Azure.Containers.Tools.Targets -Version 1.10.9
        dotnet add package Azure.Storage.Blobs -Version 12.10.1
        dotnet add package Swashbuckle.AspNetCore -Version 5.6.3
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-build --verbosity normal
